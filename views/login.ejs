<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 min-h-screen flex items-center justify-center p-4">

  <main class="w-full max-w-sm">
    <!-- Card -->
    <section class="bg-zinc-800/80 backdrop-blur-sm border border-zinc-700 rounded-3xl shadow-xl p-6 sm:p-8">
      <!-- logo / icon -->
      <div class="flex items-center justify-center mb-4">
        <div class="bg-gradient-to-br from-zinc-700 to-zinc-900 rounded-full p-3">
          <svg class="w-10 h-10 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 11c2.21 0 4-1.79 4-4S14.21 3 12 3 8 4.79 8 7s1.79 4 4 4zM6 21v-1a4 4 0 0 1 4-4h4a4 4 0 0 1 4 4v1" />
          </svg>
        </div>
      </div>

      <h1 class="text-center text-2xl font-semibold text-white mb-4 tracking-tight">Welcome back</h1>
      <p class="text-center text-sm text-zinc-400 mb-6">Sign in to continue to the students app</p>

      <% if (error) { %>
        <p class="text-red-500 text-center text-sm mb-4"><%= error %></p>
      <% } %>

      <form method="POST" action="/login" class="space-y-4">
        <label class="block">
          <span class="text-sm text-zinc-300 mb-1 block">Username</span>
          <input
            type="text"
            name="username"
            required
            class="w-full bg-zinc-900/40 border border-zinc-700 placeholder:text-zinc-500 text-white px-4 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
            placeholder="Enter username"
            aria-label="username"
          />
        </label>

        <label class="block">
          <span class="text-sm text-zinc-300 mb-1 block">Password</span>
          <input
            type="password"
            name="password"
            required
            class="w-full bg-zinc-900/40 border border-zinc-700 placeholder:text-zinc-500 text-white px-4 py-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 transition"
            placeholder="Enter password"
            aria-label="password"
          />
        </label>

        <button
          type="submit"
          class="w-full mt-1 inline-flex items-center justify-center gap-2 bg-blue-600 hover:bg-blue-700 active:scale-98 transform rounded-lg py-2 text-white font-medium transition"
        >
          <!-- subtle icon -->
          <svg class="w-4 h-4 -ml-1" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 12H3m0 0l4-4m-4 4l4 4M21 12v6a2 2 0 0 1-2 2H7" />
          </svg>
          <span>Login</span>
        </button>
      </form>

      <!-- small helper links -->
      <div class="mt-4 text-center text-xs text-zinc-400">
        <p>Default: <span class="text-zinc-200 font-medium">admin / 12345</span></p>
      </div>
    </section>

    <!-- footer / credit -->
    <footer class="mt-5 text-center">
      <p class="text-xs text-zinc-500">Created by <span class="text-zinc-300 font-medium">Ashwini</span> &amp; <span class="text-zinc-300 font-medium">Swapnil</span></p>
    </footer>
  </main>

</body>
</html>
