<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>All Students</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col items-center p-6 sm:p-10">

  <!-- Top bar -->
  <header class="w-full max-w-6xl mb-8">
    <div class="bg-zinc-800/60 backdrop-blur-sm rounded-2xl p-4 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
      <div class="flex items-center gap-4">
        <h1 class="text-3xl sm:text-4xl font-bold tracking-tight">All Students</h1>
        <span class="text-sm text-zinc-400 hidden sm:inline">
          Total: <span class="text-white font-semibold"><%= students ? students.length : 0 %></span>
        </span>
      </div>

      <!-- Search + Buttons -->
      <div class="flex flex-col sm:flex-row sm:items-center gap-3 w-full sm:w-auto">

        <!-- Search form -->
        <form action="/students/search" method="GET" class="flex w-full sm:w-auto">
          <input
            name="rollNo"
            type="text"
            placeholder="Search by roll..."
            class="w-full sm:w-64 px-3 py-2 rounded-l-lg border border-zinc-700 bg-zinc-900 text-white placeholder:text-zinc-500 
            focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
          <button type="submit" class="px-4 py-2 bg-blue-600 rounded-r-lg hover:bg-blue-700 transition">Search</button>
        </form>

        <!-- Buttons -->
        <div class="flex gap-2">
          <a href="/input" class="px-4 py-2 bg-green-600 hover:bg-green-700 rounded-lg font-medium transition">➕ Add</a>
         
          <a href="/logout" class="px-4 py-2 bg-red-600 hover:bg-red-700 rounded-lg font-medium transition">Logout</a>
        </div>

      </div>
    </div>
  </header>

  <!-- Students Grid -->
  <main class="w-full max-w-6xl">
    <% if (students && students.length > 0) { %>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <% students.forEach(student => { %>

          <article class="bg-zinc-800 p-6 rounded-2xl shadow-lg hover:shadow-2xl transition-colors duration-200 flex flex-col justify-between">

            <div>
              <h2 class="text-2xl font-semibold mb-2 leading-tight"><%= student.name %></h2>
              <p class="text-sm text-zinc-400 mb-1">
                <span class="font-medium text-zinc-200">Roll:</span> <%= student.roll %>
              </p>
              <p class="text-sm text-zinc-400 mb-1">
                <span class="font-medium text-zinc-200">Email:</span> <%= student.email || '—' %>
              </p>
              <p class="text-sm text-zinc-400">
                <span class="font-medium text-zinc-200">Mobile:</span> <%= student.mobile || '—' %>
              </p>
            </div>

            <!-- Actions -->
            <div class="mt-6 flex items-center justify-between gap-3">
              <!-- Only DELETE -->
              <form action="/students/<%= student._id %>/delete" method="POST"
                onsubmit="return confirm('Are you sure you want to delete <%= student.name %>?');">
                <button type="submit" class="px-4 py-2 bg-red-600 hover:bg-red-700 rounded-md text-sm font-medium transition">
                  Delete
                </button>
              </form>

              <time class="text-xs text-zinc-500">
                ID: <span class="text-zinc-400"><%= student._id.toString().slice(-6) %></span>
              </time>
            </div>

          </article>

        <% }) %>
      </div>

    <% } else { %>

      <!-- Empty State -->
      <div class="bg-zinc-800 p-8 rounded-xl text-center">
        <p class="text-zinc-400 text-lg mb-4">😕 No students found.</p>
        <a href="/input" class="inline-block px-6 py-2 bg-green-600 hover:bg-green-700 rounded-lg font-semibold transition">
          Add your first student
        </a>
      </div>

    <% } %>
  </main>

  <!-- Footer -->
  <footer class="w-full max-w-6xl mt-12 text-center text-zinc-500 text-sm">
    <p>Student Management App • Tailwind UI</p>
  </footer>

</body>
</html>
