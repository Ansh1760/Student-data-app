<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Student</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen">

  <!-- 🔹 Navbar -->
  <nav class="bg-zinc-800 text-white px-6 py-3 flex flex-col sm:flex-row items-center justify-between shadow-md">
    <a href="/read" class="text-lg font-semibold hover:text-blue-400 transition">
      See All Students
    </a>

    <!-- ✅ Working Search form -->
    <form action="/students/search" method="GET" class="flex items-center gap-2 w-full sm:w-auto mt-3 sm:mt-0">
      <input 
        type="text" 
        name="rollNo" 
        placeholder="Search by Roll No"
        class="px-4 py-2 rounded-lg text-black w-full sm:w-60 focus:outline-none focus:ring-2 focus:ring-blue-500"
      >
      <button 
        type="submit"
        class="bg-blue-600 px-4 py-2 rounded-lg text-white hover:bg-blue-700 transition">
        Search
      </button>
    </form>
  </nav>

  <!-- 🔹 Page Content -->
  <div class="max-w-3xl mx-auto p-8">
    <h3 class="text-3xl font-semibold tracking-tight mb-6">Create Student</h3>

    <form action="/students" method="POST" class="bg-zinc-800 p-6 rounded-xl shadow-lg flex flex-col gap-4">
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <input type="text" name="name" placeholder="Enter Name" required
          class="px-4 py-2 rounded-lg border-2 border-zinc-700 bg-zinc-900 text-white focus:border-blue-500 focus:outline-none">
        <input type="text" name="rollNo" placeholder="Enter Roll No" required
          class="px-4 py-2 rounded-lg border-2 border-zinc-700 bg-zinc-900 text-white focus:border-blue-500 focus:outline-none">
        <input type="email" name="email" placeholder="Enter Email"
          class="px-4 py-2 rounded-lg border-2 border-zinc-700 bg-zinc-900 text-white focus:border-blue-500 focus:outline-none">
        <input type="text" name="phone" placeholder="Mobile Number"
          class="px-4 py-2 rounded-lg border-2 border-zinc-700 bg-zinc-900 text-white focus:border-blue-500 focus:outline-none">
      </div>

      <div class="flex justify-end">
        <input type="submit" value="Create Student"
          class="bg-blue-600 hover:bg-blue-700 px-6 py-2 rounded-lg font-medium transition cursor-pointer">
      </div>
    </form>
  </div>

</body>
</html>
